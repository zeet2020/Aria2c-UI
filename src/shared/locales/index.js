/**
 * English-only build. (Other locales were removed in this fork.)
 */
export const availableLanguages = [
  {
    value: 'en-US',
    label: 'English'
  }
]

/**
 * getLanguage — always English in this build.
 */
export const getLanguage = () => 'en-US'
